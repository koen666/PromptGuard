# B2601 PromptGuard 软件功能拆解

## 1. 中期汇报对应的软件思路

根据《中期汇报.pdf》的表达，PromptGuard 不是一个新的大模型，也不是单纯的 Prompt 编辑器，更不是重复实现 GitHub 的文本版本管理能力。它要解决的是：当 Prompt 被放进真实 LLM 应用以后，如何像软件系统中的重要配置资产一样被记录、测试、审核、保护、试用和回退。

中期汇报中的核心判断可以归纳为四点：

| 核心判断 | 对软件功能的要求 |
|---|---|
| Prompt 是资产 | 需要统一管理、版本快照、差异对比、变更说明和历史追踪 |
| Prompt 改动会改变模型行为 | 不能只看文本 Diff，还要用测试集验证回答质量、格式稳定性、成本和延迟 |
| Prompt 也是风险点 | 需要权限控制、敏感内容检查、诱导泄露测试、审计日志和上线前审核 |
| Prompt 上线后仍可能出问题 | 需要小范围试用、运行观察、异常告警和快速回滚 |

因此，PromptGuard 的功能主线应围绕中期汇报里的四句话展开：

1. **看得见**：Prompt 不再散落在文档、聊天记录和代码常量里，而是有资产台账、版本记录、Diff 和审计日志。
2. **测得出**：Prompt 修改后必须通过典型问题和安全样例测试，能看到新旧版本在质量、格式、成本、延迟和风险上的差异。
3. **护得住**：Prompt 中包含的业务规则、产品经验、输出规范和安全边界要被权限、内容检查、诱导泄露测试和操作审计保护起来。
4. **退得回**：新版本不直接替换全部使用场景，而是先小范围试用；一旦异常，可以切回历史稳定版本。

## 2. 软件定位

PromptGuard 是一个面向 LLM 应用上层的 Prompt 管理、评测与保护平台。它不参与底层大模型训练或微调，不重写业务应用的对话界面，也不把目标停留在“保存几段 Prompt 文本”。它关注的是 Prompt 从创建、修改、测试、审核、试用到回滚的工程化闭环。

课程原型阶段的实现定位应调整为：

> **PromptGuard 核心平台能力 + 多入口交互工具 + 本地/服务端持久化 + 报告展示与集成接口**。

也就是说，PromptGuard 本质上仍然是一个软件产品，核心是 Prompt 资产管理、评测、保护、审核、试用发布和回滚能力。CLI 只是其中一种轻量入口工具，作用是让用户不依赖复杂界面也能快速使用核心功能，便于课程演示、脚本化调用和后续集成；它不等于整个产品本身。

这种定位与中期汇报中的“先跑通最核心链路”一致：先把创建 Prompt、保存版本、执行测试、查看对比结果、审核发布、小范围试用、异常回滚这条链路做扎实；交互形式可以先做轻量 CLI 和 HTML/本地报告，后续再扩展 Web 管理台、多模型深度分析、更多可视化和更细粒度权限。

CLI 入口和“多端/多场景”定位并不冲突。原型阶段可以用 CLI 表达核心操作，用 HTML/本地页面表达报告查看；后续如果扩展到 ToB 或 ToG 场景，可以在同一套应用服务之上增加 Web 管理台、开放 API 或业务系统接入端。

| 端/场景 | 原型阶段形态 | 后续扩展方向 |
|---|---|---|
| 工程操作端 | 轻量 CLI 入口、本地页面 | Web 管理台、桌面工具 |
| 报告查看端 | HTML/本地报告 | 可视化大屏、团队共享报告 |
| 业务接入端 | RoutePolicy 本地模拟 | API、SDK、配置中心接入 |
| 组织管理场景 | 本地用户和角色 | ToB/ToG 的组织、部门、租户和审计要求 |

## 3. 与 GitHub 和普通 Prompt 编辑器的区别

| 对比对象 | 能解决什么 | 解决不了什么 | PromptGuard 的补充 |
|---|---|---|---|
| GitHub | 记录文本变化、提交人、提交时间 | 无法判断 Prompt 改完后模型行为是否变差 | 对新旧 Prompt 执行同一批测试，比较质量、格式、成本、延迟和安全风险 |
| 普通 Prompt 编辑器 | 编写和保存 Prompt 文本 | 缺少审核、试用、回滚、权限和审计闭环 | 把 Prompt 当成可发布、可保护、可恢复的软件资产 |
| 业务应用内置配置 | 让某个系统读取 Prompt | 难以统一管理多个版本和跨场景评测 | 提供独立的 Prompt 生命周期管理和发布记录 |

## 4. 用户角色

| 角色 | 主要关注点 | 典型操作入口 |
|---|---|---|
| Prompt 编写人员 | 编写、修改、保存版本、发起评测、提交审核 | Web/CLI 创建 Prompt、保存版本、导入数据集、发起评测、提交审核 |
| 评审人员 | 版本差异、评测报告、安全风险、审核意见 | Web/HTML 报告/CLI 查看 Diff、评测报告，并通过或驳回 |
| 发布/运维管理员 | 小范围试用、当前生效版本、运行观察、回滚 | Web/CLI 配置灰度比例、查看状态、全量生效或快速回滚 |
| 系统管理员 | 账号权限、模型配置、阈值配置、审计追踪 | Web/CLI 管理用户、模型、阈值和审计日志 |
| 课程项目团队成员 | 查看演示结果和历史记录 | HTML 报告、本地页面或 CLI 查看 Prompt、报告和发布历史 |

## 5. 总体功能模块

PromptGuard 的功能按中期汇报思路拆为 9 个模块。交互入口不应压过产品能力本身，它负责把核心能力包装成更容易使用的 Web、CLI、报告页或 API 形式。

| 模块 | 对应价值 | 功能概述 |
|---|---|---|
| 交互入口与集成模块 | 贯穿全流程 | 提供 Web/CLI/API/HTML 报告等入口，串联 Prompt、评测、安全、审核、发布、回滚和报告 |
| 身份与权限模块 | 护得住 | 用户登录、角色权限、操作级权限拦截、高风险操作校验 |
| Prompt 资产与版本模块 | 看得见 | Prompt 创建、编辑、标签、版本快照、变更说明、版本 Diff |
| 评测数据集模块 | 测得出 | 导入 CSV/JSON 测试集，维护典型问题、期望输出和安全测试样例 |
| 自动评测与对比模块 | 测得出 | 执行新旧版本评测，计算质量、格式、成本、延迟和错误指标 |
| Prompt 保护模块 | 护得住 | 敏感内容检查、诱导泄露测试、输出脱敏、风险项记录 |
| 审核模块 | 护得住 | 提交审核、查看审核材料、通过或驳回，并记录意见 |
| 试用发布与回滚模块 | 退得回 | 灰度试用、RoutePolicy 模拟路由、全量生效记录、快速回滚 |
| 报告观察与审计模块 | 看得见、退得回 | 生成 HTML 报告、展示指标摘要、记录审计日志、输出发布和回滚历史 |

## 6. 交互入口与 CLI 工具设计

### 6.1 多入口定位

PromptGuard 应被设计成一个可通过多种入口使用的软件产品，而不是只能通过某一种界面使用的单点工具。核心业务能力沉淀在应用服务和领域服务中，上层可以根据使用场景选择不同入口。

| 入口形式 | 适合场景 | 在原型中的作用 |
|---|---|---|
| Web/本地页面 | 日常管理、报告查看、评审确认 | 展示 Prompt 列表、Diff、评测报告、审核和发布状态 |
| CLI 入口工具 | 快速操作、脚本化、演示、自动化集成 | 用命令调用核心流程，降低部署和使用门槛 |
| HTML 报告 | 汇报展示、评审材料、结果归档 | 展示评测结果、风险项、发布记录和审计摘要 |
| API/SDK | 业务系统接入、后续扩展 | 让外部 LLM 应用读取生效 Prompt 或提交评测任务 |

### 6.2 CLI 工具的职责

CLI 的定位是“入口工具”和“泛化操作通道”。它把平台能力包装成命令，让没有完整 Web 环境的用户也能使用 PromptGuard；同时便于课程演示、自动化测试和后续接入 CI/CD 流程。

CLI 需要承担以下职责：

| 职责 | 说明 |
|---|---|
| 轻量操作入口 | 用命令触发 Prompt 创建、版本保存、评测、审核、灰度试用和回滚 |
| 自动化入口 | 允许脚本或 CI 流程调用评测、报告生成和安全检查 |
| 演示入口 | 在课程项目中快速演示核心闭环，减少对复杂部署环境的依赖 |
| 集成入口 | 后续可以作为 API/SDK 之外的命令行接入方式 |
| 报告入口 | 在终端输出摘要，并打开或导出 HTML 报告 |
| 权限与审计入口 | 调用同一套权限校验和审计日志机制，不绕过平台规则 |

### 6.3 CLI 命令分组

CLI 命令建议统一使用 `promptguard`，也可以提供短别名 `pg`。这些命令只是对核心平台能力的包装，Web 页面和 API 入口可以映射到同一批应用服务。

| 命令组 | 命令示例 | 对应功能 |
|---|---|---|
| `auth` | `promptguard auth login`、`promptguard auth whoami` | 登录、查看当前用户、退出登录 |
| `prompt` | `promptguard prompt create`、`promptguard prompt edit`、`promptguard prompt list` | Prompt 资产创建、编辑和查询 |
| `version` | `promptguard version save`、`promptguard version diff`、`promptguard version history` | 保存不可变版本、查看差异和历史 |
| `dataset` | `promptguard dataset import`、`promptguard dataset preview` | 导入和预览评测数据集 |
| `eval` | `promptguard eval run`、`promptguard eval compare`、`promptguard eval report` | 执行评测、新旧版本对比、生成报告 |
| `security` | `promptguard security scan`、`promptguard security attack-test` | 敏感内容检查和诱导泄露测试 |
| `review` | `promptguard review submit`、`promptguard review approve`、`promptguard review reject` | 提交审核和处理审核意见 |
| `release` | `promptguard release gray`、`promptguard release promote`、`promptguard release rollback`、`promptguard release status`、`promptguard release history` | 灰度试用、全量生效记录、回滚、状态查看和发布历史 |
| `report` | `promptguard report open`、`promptguard report export` | 打开或导出 Diff、评测、发布和审计报告 |
| `user` | `promptguard user create`、`promptguard user grant`、`promptguard user list` | 本地用户和角色授权管理 |
| `config` | `promptguard config model`、`promptguard config threshold` | 模型、阈值和系统参数配置 |
| `audit` | `promptguard audit list`、`promptguard audit export` | 审计日志查询和导出 |

### 6.4 入口输出规范

| 场景 | CLI/页面摘要 | 文件/报告输出 |
|---|---|---|
| 保存版本 | 版本号、变更说明、Diff 摘要 | 版本快照记录 |
| 执行评测 | 任务状态、通过率、平均延迟、Token 消耗、风险数量 | HTML 评测报告、样例级结果 |
| 安全检查 | 命中规则、风险等级、建议处理方式 | 风险项记录、脱敏后的证据片段 |
| 提交审核 | 审核单号、关联版本、评测报告路径 | 审核记录 |
| 灰度试用 | 版本号、灰度比例、RoutePolicy 状态 | 发布记录、当前生效策略 |
| 快速回滚 | 目标版本、回滚原因、切换结果 | 回滚记录、审计日志 |

## 7. 功能点详细拆解

### 7.1 身份与权限模块

目标：保证 Prompt 的查看、修改、审核、发布、回滚和配置操作不是任何人都可以随意执行。

| 功能点 | 输入 | 输出 | 关键规则 |
|---|---|---|---|
| 本地用户登录 | 用户名、密码 | 会话令牌、当前角色 | 登录失败达到阈值后锁定；失败写入审计日志 |
| 当前用户查询 | 会话令牌 | 用户信息、角色、可执行操作 | 会话过期时拒绝访问 |
| 操作级权限控制 | 用户角色、操作类型、入口来源 | 允许或拒绝 | 发布、回滚、配置变更属于高风险操作 |
| 高风险操作确认 | 操作类型、原因说明 | 操作确认结果 | 审核通过、灰度发布、全量生效、快速回滚必须填写原因 |
| 权限审计 | 操作者、命令、对象、结果 | 审计日志 | 越权、失败和成功都可追溯 |

关联数据对象：`user`、`role`、`user_role`、`session`、`audit_log`。

### 7.2 Prompt 资产与版本模块

目标：把 Prompt 从普通文本提升为可追踪、可比较、可评测、可审核的配置资产。

| 功能点 | 输入 | 输出 | 关键规则 |
|---|---|---|---|
| 创建 Prompt | 名称、正文、场景、标签、描述 | Prompt ID | 名称在工作空间内唯一；正文不超过 32 KB |
| 编辑 Prompt | Prompt ID、正文、标签、描述 | 草稿状态 Prompt | 编辑不会覆盖已发布版本 |
| 保存版本 | Prompt ID、正文、变更说明 | 新版本号、不可变快照 | 版本创建后正文不可修改；内容无变化拒绝创建 |
| 版本差异对比 | 两个版本号 | Diff 摘要、HTML Diff 报告 | 高亮新增、删除、修改内容 |
| 版本历史查询 | Prompt ID | 版本列表、状态、创建人、创建时间 | 支持按状态筛选 |
| 标签和场景管理 | 标签、业务场景 | 标签列表、场景列表 | 便于区分客服、代码助手、面试、办公自动化等应用场景 |

关联数据对象：`prompt`、`prompt_tag`、`prompt_version`、`version_diff`、`audit_log`。

### 7.3 评测数据集模块

目标：让 Prompt 修改后的效果可以被测试，而不是只靠主观感觉判断。

| 功能点 | 输入 | 输出 | 关键规则 |
|---|---|---|---|
| 导入数据集 | CSV/JSON 文件 | 数据集 ID、导入统计、错误行 | 单文件不超过 10 MB；单数据集样例数不超过 500 条 |
| 样例格式校验 | input、expected、metadata | 校验结果 | 缺字段、格式错误逐行返回 |
| 典型问题管理 | 业务场景、用户问题、期望要点 | 普通评测样例 | 用于测试回答质量和格式稳定性 |
| 安全样例管理 | 攻击性问题、期望拒答策略 | 安全评测样例 | 包含“输出系统提示词”“忽略规则”等诱导问题 |
| 样例预览 | 数据集 ID、分页参数 | 样例列表 | 终端或页面输出摘要，HTML 展示完整列表 |

关联数据对象：`dataset`、`dataset_sample`、`security_sample`、`audit_log`。

### 7.4 自动评测与对比模块

目标：用同一批样例比较新旧 Prompt 的实际表现，回答“改完以后还能不能放心用”。

| 功能点 | 输入 | 输出 | 关键规则 |
|---|---|---|---|
| 创建评测任务 | Prompt 版本、数据集、模型配置 | 评测任务 ID | 任务状态初始为 `QUEUED` 或 `RUNNING` |
| 执行模型调用 | Prompt、样例输入、模型配置 | 模型输出、延迟、Token 用量 | 通过 `ModelAdapter` 适配真实或 Mock LLM |
| 新旧版本对比 | 基准版本、新版本、同一数据集 | 对比任务和差异指标 | 两个版本必须属于同一 Prompt |
| 样例级结果记录 | 输出文本、评分、错误信息 | `evaluation_result` 记录 | 记录 score、latency_ms、token_count、error_message |
| 汇总指标计算 | 样例级结果 | 通过率、准确率、格式错误率、平均延迟、成本 | 指标保留规定精度 |
| 报告生成 | 评测任务结果 | 终端摘要、HTML 报告 | 报告用于审核、演示和复盘 |
| 失败重试 | 失败任务 ID | 重新执行结果 | 外部模型超时、限流、鉴权失败统一封装 |

关联数据对象：`evaluation_task`、`evaluation_result`、`model_config`、`dataset_sample`。

### 7.5 Prompt 保护模块

目标：体现中期汇报中“Prompt 不仅要管理，还要保护”的重点。

| 功能点 | 输入 | 输出 | 关键规则 |
|---|---|---|---|
| Prompt 正文敏感内容检查 | Prompt 正文 | 风险项列表 | API Key、连接串、内部账号、过细业务规则等上线前提醒 |
| 模型输出敏感内容检查 | 模型输出文本 | 泄露风险标记 | 不展示完整密钥、堆栈和连接串 |
| 诱导泄露测试 | 安全测试样例 | 安全评分、风险证据 | 检查是否泄露系统提示词、内部评分标准或权限规则 |
| 风险等级判定 | 命中规则、证据片段 | 低/中/高风险 | 高风险版本不能直接进入发布 |
| 脱敏展示 | 错误信息、模型输出、配置值 | 脱敏文本 | 报告中只保留必要证据 |

关联数据对象：`security_rule`、`security_finding`、`evaluation_result`、`audit_log`。

### 7.6 人工审核模块

目标：在 Prompt 试用或发布前，让评审人员基于版本差异、评测结果和安全风险做人工确认。

| 功能点 | 输入 | 输出 | 关键规则 |
|---|---|---|---|
| 提交审核 | Prompt 版本、评测报告、变更说明 | `PENDING` 审核记录 | 待审核版本必须已完成评测 |
| 查看审核材料 | Diff、评测报告、安全风险、变更说明 | 审核依据 | 终端摘要或 HTML 报告展示详细内容 |
| 审核通过 | 审核意见 | `APPROVED` 状态 | 通过后版本进入可试用状态 |
| 审核驳回 | 驳回意见 | `REJECTED` 状态 | 驳回意见保留，版本不能发布 |
| 审核审计 | 审核人、结论、时间 | 审计日志 | 审核记录不可删除 |

关联数据对象：`review_record`、`prompt_version`、`evaluation_task`、`security_finding`、`audit_log`。

### 7.7 试用发布与快速回滚模块

目标：落实中期汇报中的“先小范围试水，没问题再推广；一旦出问题，可以马上撤回来”。

课程原型不必真实接入生产流量，可以用 `route_policy` 或本地策略文件模拟当前生效版本和灰度比例。

| 功能点 | 输入 | 输出 | 关键规则 |
|---|---|---|---|
| 灰度试用发布 | 已审核版本、环境、灰度比例、发布原因 | 发布记录、RoutePolicy | 版本状态必须为 `APPROVED`；比例 0%-100%，步进 1% |
| 试用状态查看 | Prompt ID、环境 | 当前生效版本、灰度比例、发布时间 | CLI、本地页面或 Web 页面均可展示当前策略 |
| 全量生效记录 | 目标版本、原因 | 当前版本更新、发布记录 | 灰度观察稳定后才建议全量 |
| 快速回滚 | 历史稳定版本、回滚原因 | 当前策略切换、回滚记录 | 目标版本必须曾通过审核且成功发布 |
| 发布失败保护 | 路由更新异常 | 失败记录、告警 | 更新失败时保持原生效版本 |
| 回滚失败告警 | 切换失败原因 | 高优先级告警 | 回滚失败时不能破坏当前可用状态 |

关联数据对象：`release_record`、`route_policy`、`metric_sample`、`alert_record`、`audit_log`。

### 7.8 报告观察与审计模块

目标：把 Prompt 修改、测试、试用和回滚过程变成可查看、可复盘、可验收的证据。

| 功能点 | 输入 | 输出 | 关键规则 |
|---|---|---|---|
| 版本 Diff 报告 | 两个 Prompt 版本 | HTML Diff 报告 | 高亮变化内容 |
| 评测报告 | 评测任务结果 | HTML 评测报告、终端摘要 | 展示通过率、格式错误率、延迟、成本、风险项 |
| 试用观察报告 | 发布记录、模拟指标 | 指标趋势和异常摘要 | 原型阶段可由模拟数据生成 |
| 告警判断 | 指标样本、阈值规则 | 告警记录 | 成本升高、延迟异常、格式错误率升高、安全风险可触发 |
| 审计日志查询 | 操作者、对象、时间范围 | 审计列表 | 支持分页查询 |
| 审计日志导出 | 查询条件 | CSV 或 HTML 报告 | 关键操作追加写入，不允许业务层删除 |

关联数据对象：`report_record`、`metric_sample`、`alert_rule`、`alert_record`、`audit_log`。

### 7.9 系统配置模块

目标：让模型、阈值、角色和运行参数可配置，支撑本地原型演示和后续扩展。

| 功能点 | 输入 | 输出 | 关键规则 |
|---|---|---|---|
| 模型配置 | provider、endpoint、API Key、enabled | 模型配置记录 | API Key 加密或脱敏保存 |
| Mock 模型配置 | 模型名称、模拟策略 | Mock 调用结果 | 便于无外部 API 时完成课程演示 |
| 告警阈值配置 | 指标名、比较符、阈值、级别 | 告警规则 | 启停规则可配置 |
| 角色权限配置 | 用户、角色 | 权限关系 | 高风险操作仅管理员可配置 |
| 工作空间配置 | 数据目录、报告目录、默认模型 | 配置文件 | 交互入口启动时加载 |

关联数据对象：`model_config`、`system_config`、`alert_rule`、`role`、`user_role`。

## 8. 核心业务流程

### 8.1 从 Prompt 修改到小范围试用的主流程

下面是 PromptGuard 的产品主链路。CLI 命令只作为其中一种入口示例，Web 页面或 API 也应调用同一套应用服务完成相同流程。

1. Prompt 编写人员通过 Web、CLI 或 API 登录系统。
2. 创建或编辑 Prompt，填写名称、正文、场景、标签和描述。
3. 保存新版本并填写变更说明，系统生成不可变版本快照。
4. 查看新旧版本差异，形成 Diff 摘要或 HTML Diff 报告。
5. 导入或选择评测数据集。
6. 执行新旧版本评测，CLI 示例为 `promptguard eval compare`。
7. 系统同步执行敏感内容检查和诱导泄露测试。
8. 用户查看摘要和 HTML 评测报告。
9. Prompt 编写人员提交人工审核。
10. 评审人员查看 Diff、评测报告、安全风险和变更说明。
11. 评审人员通过或驳回审核。
12. 发布/运维管理员执行小范围试用，CLI 示例为 `promptguard release gray --traffic 10`。
13. 系统更新 RoutePolicy，记录当前生效版本、灰度比例和发布原因。
14. 系统生成试用观察指标和告警记录。
15. 如果表现稳定，记录全量生效。
16. 如果出现问题，切回历史稳定版本，CLI 示例为 `promptguard release rollback`。

### 8.2 自动评测流程

1. 交互入口层接收版本、数据集、模型和报告参数。
2. `EvaluationApplicationService` 创建评测任务。
3. 任务执行器加载 `PromptVersion`、`DatasetSample` 和 `ModelConfig`。
4. 对每条样例调用 `ModelAdapter`，获得模型输出、延迟和 Token 用量。
5. 评分器比较输出和期望要点，检查格式稳定性。
6. 安全检查器扫描输出内容，并执行诱导泄露样例。
7. `MetricCalculator` 汇总通过率、准确率、格式错误率、平均延迟、Token 消耗和风险数量。
8. 任务状态变更为 `SUCCEEDED` 或 `FAILED`。
9. 系统向入口层返回摘要，并生成 HTML 评测报告。

### 8.3 Prompt 保护流程

1. 保存版本前，系统扫描 Prompt 正文中的敏感内容。
2. 评测时，系统在普通样例外追加安全测试样例。
3. 对模型输出执行敏感内容和诱导泄露检查。
4. 高风险发现写入 `security_finding`，并在评测报告中标记。
5. 版本提交审核时，评审人员必须看到风险项摘要。
6. 高风险未处理的版本不能直接进入灰度试用。

### 8.4 试用发布与回滚流程

1. 发布前校验版本状态必须为 `APPROVED`。
2. 校验灰度比例是否在 0%-100% 且按 1% 步进。
3. 创建待发布记录，写入发布原因。
4. 更新 `route_policy` 或本地策略文件。
5. 更新成功后发布记录转为 `ACTIVE`，写入审计日志。
6. 更新失败时发布记录转为 `FAILED`，当前生效版本保持不变。
7. 若观察指标异常，管理员选择历史稳定版本并填写回滚原因。
8. 回滚服务校验目标版本曾通过审核且成功发布。
9. 系统将 RoutePolicy 指向目标版本。
10. 回滚成功写入记录；失败时保持当前版本并生成高优先级告警。

## 9. 功能与数据对象关系

| 功能模块 | 主要数据表/对象 |
|---|---|
| 交互入口与集成 | `command_context`、`api_session`、`report_record`、`audit_log`、本地配置文件 |
| 身份与权限 | `user`、`role`、`user_role`、`session`、`audit_log` |
| Prompt 资产与版本 | `prompt`、`prompt_tag`、`prompt_version`、`version_diff` |
| 评测数据集 | `dataset`、`dataset_sample`、`security_sample` |
| 自动评测与对比 | `evaluation_task`、`evaluation_result`、`model_config` |
| Prompt 保护 | `security_rule`、`security_finding`、`evaluation_result` |
| 人工审核 | `review_record`、`prompt_version`、`evaluation_task` |
| 试用发布与回滚 | `release_record`、`route_policy`、`metric_sample`、`alert_record` |
| 报告观察与审计 | `report_record`、`audit_log`、`alert_rule` |
| 系统配置 | `system_config`、`model_config`、`role`、`user_role` |

## 10. 原型实现优先级

| 优先级 | 功能 | 原因 |
|---|---|---|
| P0 | 基础交互入口、Prompt 创建/编辑、版本保存、版本 Diff | 对应“看得见”，先把产品核心对象和一种最小可用入口跑通，入口可采用轻量 CLI 或本地页面 |
| P0 | 数据集导入、自动评测、新旧版本对比、HTML 评测报告 | 对应“测得出”，体现与 GitHub 的核心差异 |
| P0 | 人工审核、灰度试用、RoutePolicy 模拟、快速回滚 | 对应“退得回”，跑通中期汇报中的主链路 |
| P0 | 角色权限、审计日志、敏感内容检查、诱导泄露测试 | 对应“护得住”，体现 Prompt 保护重点 |
| P1 | Web/本地页面增强、CLI 体验优化、API/SDK 入口 | 提升易用性、脚本化能力和多端接入能力 |
| P1 | 多模型对比、运行观察图表、告警规则配置、报告导出优化 | 增强演示效果和工程完整性 |
| P2 | 复杂租户权限、真实生产流量接入、对象存储、消息队列、PostgreSQL | 作为后续扩展，不是课程原型首要目标 |

## 11. 一句话功能概括

PromptGuard 的核心功能是：围绕 Prompt 资产建立“创建/编辑 -> 版本快照 -> Diff 对比 -> 数据集评测 -> 安全检查 -> 人工审核 -> 小范围试用 -> 观察告警 -> 快速回滚 -> 审计追踪”的完整工程化管理与保护闭环，并通过 Web、CLI、HTML 报告和 API 等入口让不同用户更容易使用这套能力。
